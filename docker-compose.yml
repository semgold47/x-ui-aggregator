services:
  nginx-proxy:
    build: .
    container_name: nginx_proxy_sub
    ports:
      - "1222:443"
    env_file:
      - .env
    volumes:
      - ./config_fetcher.lua:/etc/nginx/lua/config_fetcher.lua
      - ./ssl:/etc/nginx/ssl
    restart: unless-stopped
